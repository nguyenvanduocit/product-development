<template><p>All our Feature Flags’ names are combined with <code>featureName</code> + <code>Enabled</code> to make sure that when one is on, it’s enabled. In our previous example — <code>RemovedManualOnboardingEnabled</code> — it’s easy to understand what happens when this Feature Flag is ON.</p>
</template>
