<template><h1 id="zepto-js-–-a-minimalist-javascript-library" tabindex="-1"><a class="header-anchor" href="#zepto-js-–-a-minimalist-javascript-library" aria-hidden="true">#</a> Zepto.js – a minimalist JavaScript library</h1>
<p>Zepto is a minimalist JavaScript library for modern browsers with a
largely jQuery-compatible API. If you use jQuery, you already know how to use Zepto.</p>
<p>See <a href="http://zeptojs.com">zeptojs.com</a> for an extended introduction, downloads
and documentation.</p>
<p>Zepto.js is licensed under the terms of the MIT License.</p>
<p>Want to give us money or a tip? Don't.
Instead please donate to <a href="http://charitywater.org/">charity: water</a>.</p>
<h2 id="building" tabindex="-1"><a class="header-anchor" href="#building" aria-hidden="true">#</a> Building</h2>
<p><a href="http://travis-ci.org/madrobby/zepto"><img src="https://secure.travis-ci.org/madrobby/zepto.svg?branch=master" alt="Build Status"></a></p>
<p>The official site offers a download of the default distribution of Zepto. This
is good for starting out. However, at some point you might want to add some
optional modules and remove some of the default ones you don't need, to keep the
size at a minimum. That's when you need to check out Zepto's source code and use
the build commands. Alternatively you can use the web based <a href="http://github.e-sites.nl/zeptobuilder/">Zepto Builder</a>.</p>
<p>You will need Node.js installed on your system.</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>$ <span class="token function">npm</span> <span class="token function">install</span>
$ <span class="token function">npm</span> run-script dist

<span class="token comment"># do a custom build</span>
$ <span class="token assign-left variable">MODULES</span><span class="token operator">=</span><span class="token string">"zepto event data"</span> <span class="token function">npm</span> run-script dist

<span class="token comment"># on Windows</span>
c:<span class="token punctuation">\</span>zepto<span class="token operator">></span> SET <span class="token assign-left variable">MODULES</span><span class="token operator">=</span>zepto event data
c:<span class="token punctuation">\</span>zepto<span class="token operator">></span> <span class="token function">npm</span> run-script dist
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><p>The resulting files are:</p>
<ol>
<li><code>dist/zepto.js</code></li>
<li><code>dist/zepto.min.js</code></li>
</ol>
<p>If you install CoffeeScript globally, you can run <code>make</code> directly:</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code><span class="token comment"># one-time operation</span>
$ <span class="token function">npm</span> <span class="token function">install</span> coffee-script --global

$ coffee <span class="token function">make</span> dist
$ <span class="token assign-left variable">MODULES</span><span class="token operator">=</span><span class="token string">"zepto event data ..."</span> ./make dist

<span class="token comment"># on Windows</span>
c:<span class="token punctuation">\</span>zepto<span class="token operator">></span> SET <span class="token assign-left variable">MODULES</span><span class="token operator">=</span>zepto event data
c:<span class="token punctuation">\</span>zepto<span class="token operator">></span> coffee <span class="token function">make</span> dist
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br><span class="line-number">2</span><br><span class="line-number">3</span><br><span class="line-number">4</span><br><span class="line-number">5</span><br><span class="line-number">6</span><br><span class="line-number">7</span><br><span class="line-number">8</span><br><span class="line-number">9</span><br></div></div><h2 id="zepto-modules" tabindex="-1"><a class="header-anchor" href="#zepto-modules" aria-hidden="true">#</a> Zepto modules</h2>
<p>Zepto modules are individual files in the &quot;src/&quot; directory.</p>
<table>
<thead><tr>
  <th>module</th> <th>default</th> <th>description</th>
</tr></thead>
<tbody>
  <tr>
    <th><a href="src/zepto.js#files">zepto</a></th>
    <td>✔</td>
    <td>Core module; contains most methods</td>
  </tr>
  <tr>
    <th><a href="src/event.js#files">event</a></th>
    <td>✔</td>
    <td>Event handling via <code>on()</code> &amp; <code>off()</code></td>
  </tr>
  <tr>
    <th><a href="src/ajax.js#files">ajax</a></th>
    <td>✔</td>
    <td>XMLHttpRequest and JSONP functionality</td>
  </tr>
  <tr>
    <th><a href="src/form.js#files">form</a></th>
    <td>✔</td>
    <td>Serialize &amp; submit web forms</td>
  </tr>
  <tr>
    <th><a href="src/ie.js#files">ie</a></th>
    <td>✔</td>
    <td>Support for Internet Explorer 10+ on the desktop and Windows Phone 8</td>
  </tr>
  <tr>
    <th><a href="src/detect.js#files">detect</a></th>
    <td></td>
    <td>Provides <code>$.os</code> and <code>$.browser</code> information</td>
  </tr>
  <tr>
    <th><a href="src/fx.js#files">fx</a></th>
    <td></td>
    <td>The <code>animate()</code> method</td>
  </tr>
  <tr>
    <th><a href="src/fx_methods.js#files">fx_methods</a></th>
    <td></td>
    <td>
      Animated <code>show</code>, <code>hide</code>, <code>toggle</code>,
      and <code>fade*()</code> methods.
    </td>
  </tr>
  <tr>
    <th><a href="src/assets.js#files">assets</a></th>
    <td></td>
    <td>
      Experimental support for cleaning up iOS memory after removing
      image elements from the DOM.
    </td>
  </tr>
  <tr>
    <th><a href="src/data.js#files">data</a></th>
    <td></td>
    <td>
      A full-blown <code>data()</code> method, capable of storing arbitrary
      objects in memory.
    </td>
  </tr>
  <tr>
    <th><a href="src/deferred.js#files">deferred</a></th>
    <td></td>
    <td>
      Provides <code>$.Deferred</code> promises API.
      Depends on the "callbacks" module.
    </td>
  </tr>
  <tr>
    <th><a href="src/callbacks.js#files">callbacks</a></th>
    <td></td>
    <td>
      Provides <code>$.Callbacks</code> for use in "deferred" module.
    </td>
  </tr>
  <tr>
    <th><a href="src/selector.js#files">selector</a></th>
    <td></td>
    <td>
      Experimental <a href="http://api.jquery.com/category/selectors/jquery-selector-extensions/">jQuery
      CSS extensions</a> support for functionality such as <code>$('div:first')</code> and
      <code>el.is(':visible')</code>.
    </td>
  </tr>
  <tr>
    <th><a href="src/touch.js#files">touch</a></th>
    <td></td>
    <td>
      Fires tap– and swipe–related events on touch devices. This works with both
      `touch` (iOS, Android) and `pointer` events (Windows Phone).
    </td>
  </tr>
  <tr>
    <th><a href="src/gesture.js#files">gesture</a></th>
    <td></td>
    <td>Fires pinch gesture events on touch devices</td>
  </tr>
  <tr>
    <th><a href="src/stack.js#files">stack</a></th>
    <td></td>
    <td>Provides <code>andSelf</code> &amp; <code>end()</code> chaining methods</td>
  </tr>
  <tr>
    <th><a href="src/ios3.js#files">ios3</a></th>
    <td></td>
    <td>
      String.prototype.trim and Array.prototype.reduce methods
      (if they are missing) for compatibility with iOS 3.x.
    </td>
  </tr>
</tbody>
</table>
<h2 id="contributing" tabindex="-1"><a class="header-anchor" href="#contributing" aria-hidden="true">#</a> Contributing</h2>
<p>Please read our <a href="https://github.com/madrobby/zepto/blob/master/CONTRIBUTING.md">contribution guidelines</a>
for information on how to contribute.</p>
<p>Get in touch:</p>
<ul>
<li>@<a href="http://twitter.com/zeptojs">zeptojs</a></li>
</ul>
<h3 id="write-documentation" tabindex="-1"><a class="header-anchor" href="#write-documentation" aria-hidden="true">#</a> Write documentation</h3>
<p>Zepto docs are written in Markdown and live in the <a href="https://github.com/madrobby/zepto/tree/gh-pages#readme">&quot;gh-pages&quot; branch</a>.
They are published on <a href="http://zeptojs.com">zeptojs.com</a>.</p>
<p>You can use GitHub's web interface to make quick changes to documentation for
specific Zepto features
(<a href="https://github.com/madrobby/zepto/blob/gh-pages/ajax/_posts/1900-01-01-Z-ajaxSettings.md">example: ajaxSettings</a>).
This will submit a pull request to us that we can review.</p>
<h3 id="report-a-bug" tabindex="-1"><a class="header-anchor" href="#report-a-bug" aria-hidden="true">#</a> Report a bug</h3>
<ol>
<li>Check if the bug is already fixed in the master branch since the last release.</li>
<li>Check <a href="https://github.com/madrobby/zepto/issues">existing issues</a>. Open a new one, including exact browser &amp;
platform information. For better formatting of your report, see
<a href="https://help.github.com/articles/creating-and-highlighting-code-blocks/">GitHub-flavored Markdown</a>.</li>
</ol>
<h3 id="running-tests" tabindex="-1"><a class="header-anchor" href="#running-tests" aria-hidden="true">#</a> Running tests</h3>
<p>You will need to install <a href="http://phantomjs.org/download.html">PhantomJS</a>. On OS X, that's easy:</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>$ brew <span class="token function">install</span> phantomjs
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>To run the automated tests:</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>$ <span class="token function">npm</span> <span class="token builtin class-name">test</span>
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>To run a test server, which you can hit with your browsers and devices:</p>
<div class="language-bash ext-sh line-numbers-mode"><pre v-pre class="language-bash"><code>$ <span class="token function">npm</span> start
</code></pre><div class="line-numbers" aria-hidden="true"><span class="line-number">1</span><br></div></div><p>Go to <code>http://your-ip-address:3000/</code> on your browser and follow the
instructions. For your convenience test failures and exceptions will be
reported to the the console you started the test server in (as well as
the browser console if available).</p>
</template>
